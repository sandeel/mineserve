packages: 
  yum:
    git: []

commands:
  install_mcrcon:
    command: "rm -rf mcrcon && git clone https://github.com/Tiiffi/mcrcon.git && cd mcrcon && gcc -pedantic -Wall -Wextra -O2 -s -o mcrcon mcrcon.c && chmod 777 -R /home/ec2-user"
    cwd: /home/ec2-user
